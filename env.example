# LinkedIn app credentials
# Use canonical keys below; aliases are supported by scripts/sync-secrets.js
EXPO_PUBLIC_LINKEDIN_CLIENT_ID=<your-linkedin-client-id>
EXPO_SECRET_LINKEDIN_CLIENT_SECRET=<your-linkedin-client-secret>

# LinkedIn redirect (match what is registered in LinkedIn Dev portal)
# Choose ONE per environment:
# - Web/local: http://localhost:8081/auth/linkedin/callback
# - Expo Go / simulator: exp://localhost:8081/--/auth/linkedin/callback
# - Production web: https://1protip.com/auth/linkedin/callback (or https://app.1protip.com/auth/linkedin/callback)
EXPO_PUBLIC_LINKEDIN_REDIRECT_URI=http://localhost:8081/auth/linkedin/callback

# Backend feed endpoint (Firebase Hosting rewrite to functions)
# Hard-coded in app to /api/feed; no env override needed

