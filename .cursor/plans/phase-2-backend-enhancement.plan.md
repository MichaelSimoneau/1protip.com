# Phase 2: Backend Feed Enhancement

This phase focuses on building out the complete, paginated server-side feed.

## Goals
- Implement server-side pagination for the feed API.
- Update `useFeed` hook to support cursor-based pagination.
- Ensure efficient data fetching and caching.

## Todos
- [x] Update `apps/free/services/linkedin/feed.ts` to request paginated feed.
- [x] Update `apps/free/features/feed/hooks/useFeed.ts` to handle pagination state.
- [x] Implement backend logic for feed pagination (if needed).
